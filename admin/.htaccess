# Temporär ohne HTTP Basic Auth - nur PHP Session Auth
<Files "*.php">
    Order allow,deny
    Allow from all
</Files>

# Verstecke .htpasswd trotzdem
<Files ".htpasswd">
    Order deny,allow
    Deny from all
</Files>